apiVersion: v2
name: opensearch-bootstrap
description: One-shot Job that applies the GoSat index template and creates the 'gosat-*' index pattern in OpenSearch Dashboards.
type: application
version: 0.1.0
appVersion: "1.0"
